version: "3.7"
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: postgres
    ports: 
      - '5432:5432'
    volumes:
      - chanatornvolume:/var/lib/postgresql/data
        
volumes:
  chanatornvolume: